// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const mapFilter = (filters) => {
    var _a, _b;
    return filters.filters
        ? {
            combine: mapFilterCombineOperator(filters.operator),
            filters: filters.filters.map(mapFilter),
        }
        : Object.assign(Object.assign({}, filters), { target: (_b = (_a = filters.target) === null || _a === void 0 ? void 0 : _a.property) !== null && _b !== void 0 ? _b : filters.target });
};
const mapFilterCombineOperator = (operator) => {
    switch (operator) {
        case "And":
            return "and";
        case "Or":
            return "or";
        default:
            return operator;
    }
};
